import React,{useContext} from "react"; import { TaskContext } from "../context/TaskContext.jsx"; import SubtaskList from "./SubtaskList.jsx"; export default function TaskCard({task}){ const {moveTask}=useContext(TaskContext); const statuses=["Todo","In Progress","Done"]; return (<div className="bg-white dark:bg-gray-700 p-2 mb-2 rounded"><h3 className="font-semibold">{task.title}</h3><SubtaskList task={task} /><div className="mt-2 flex gap-1">{statuses.filter(s=>s!==task.status).map(s=>(<button key={s} onClick={()=>moveTask(task.id,s)} className="px-2 py-1 bg-blue-500 text-white rounded text-sm">Move to {s}</button>))}</div></div>);}