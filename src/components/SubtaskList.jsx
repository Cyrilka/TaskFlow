import React,{useContext} from "react"; import { TaskContext } from "../context/TaskContext.jsx"; export default function SubtaskList({task}){ const {updateTask}=useContext(TaskContext); const toggle=(i)=>{ const newSubtasks=[...task.subtasks]; newSubtasks[i].done=!newSubtasks[i].done; updateTask(task.id,{subtasks:newSubtasks}); }; return (<ul className="pl-4 mt-2">{task.subtasks?.map((st,i)=>(<li key={i}><label className="flex items-center gap-2"><input type="checkbox" checked={st.done} onChange={()=>toggle(i)} />{st.title}</label></li>))}</ul>);}