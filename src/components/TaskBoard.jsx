import React,{useContext} from "react"; import { TaskContext } from "../context/TaskContext.jsx"; import TaskCard from "./TaskCard.jsx"; export default function TaskBoard(){ const {tasks}=useContext(TaskContext); const statuses=["Todo","In Progress","Done"]; return <div className="grid grid-cols-3 gap-4">{statuses.map(status=>(<div key={status} className="bg-gray-200 dark:bg-gray-800 p-2 rounded"><h2 className="font-bold mb-2">{status}</h2>{tasks.filter(t=>t.status===status).map(task=><TaskCard key={task.id} task={task}/> )}</div>))}</div>;}